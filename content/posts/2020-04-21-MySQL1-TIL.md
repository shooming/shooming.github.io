---
title: MySql TIL
date: "2020-04-21T23:00:00"
template: "post"
draft: false # 공개여부
slug: "TIL-Data MySql"
category: "MySql" # 카테고리
description: "MySql TIL 배운거 정리"
socialImage: ""
tags:
    - MySql
    - TIL
---

# MySql

1. mysql-server 패키지 설치

```
sudo apt install -y mysql-server
```
apt를 사용하여 mysql-server 패키지를 설치한다.

2. mysql 서버 초기화
```python
sudo mysql_secure_installation
```
설치 완료후 서버를 한번 초기화 시켜준다.

```bash
VALIDATE PASSWORD PLUGIN can be used to test passwords
and improve security. It checks the strength of password
and allows the users to set only those passwords which are
secure enough. Would you like to setup VALIDATE PASSWORD plugin?

Press y|Y for Yes, any other key for No: No
```
설치를 하다보면 충분히 강한 패스워드를 생성할 수있는 플러그인을 활성화 하는지 묻는데 No를 입력한다.(원칙은 YES임) yes를 적용하면 간단한 패스워드를 적용 못 한다.

```bash
Please set the password for root here.

New password: 

Re-enter new password: 
```
root 계정의 패스워드를 작성한다.

```bash
By default, a MySQL installation has an anonymous user,
allowing anyone to log into MySQL without having to have
a user account created for them. This is intended only for
testing, and to make the installation go a bit smoother.
You should remove them before moving into a production
environment.

Remove anonymous users? (Press y|Y for Yes, any other key for No) :y
```
익명의 사용자를 제가하는 것을 묻는것으로 y를 입력한다.

```bash
Normally, root should only be allowed to connect from
'localhost'. This ensures that someone cannot guess at
the root password from the network.

Disallow root login remotely? (Press y|Y for Yes, any other key for No) : n
```
root계정의 외부, 원격로그인을 허용하는지 묻는 내용이다.

```bash
By default, MySQL comes with a database named 'test' that
anyone can access. This is also intended only for testing,
and should be removed before moving into a production
environment.


Remove test database and access to it? (Press y|Y for Yes, any other key for No) : y
```
test 데이터베이스를 삭제할지 묻는 내용

```bash
Reloading the privilege tables will ensure that all changes
made so far will take effect immediately.

Reload privilege tables now? (Press y|Y for Yes, any other key for No) : y
```
privileges table을 다시 로드 할지 묻는 내용

```bash
sudo /etc/init.d/mysql restart
```
설정이 완료되면 MySql 데몬을 다시 실행한다.

3. MySql 설정
```bash
sudo mysql -u root -p
```
MySql 서버에 접속한다.

4. mysql root 로그인시 sudo 안쓰기
```mysql
mysql > use mysql;
mysql > ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '내가원하는 패스워드';
mysql > FLUSH PRIVILEGES;
```
패스워드는 root의 패스워드가 변경되는 것이므로 잘 설정해야한다.

5. Django MySql 연결하기

```bash
# 우분투에서 mysqlclient 설치시 오류가 발생한다.
Command "python setup.py egg_info" failed with error code 1 in /tmp/pip-install-zbw18e9_/mysqlclient/
# gcc도 설치해본다.
sudo apt install gcc
  
# 또는 
  
/bin/sh: 1: mysql_config: not found

# 이때
sudo apt-get install libmysqlclient-dev 
# 다시 
pip install mysqlclient
```

6. Mysql에 장고에서 사용할 데이터 베이스 만들기
```bash
mysql -u root -p
```
mysql에 접속한다.

```bash
create database 데이터베이스이름 character set utf8mb4 collate utf8mb4_general_ci;
```
이렇게하면 데이터베이스를 만들 수 있다. 만들어진 데이터베이스의 내용을 my_settings.py에 작성한다.